{
  "name": "multi-targeter",
  "private": true,
  "dependencies": {
    "serverless": "^1.48.2",
    "serverless-haskell": "^0.8.8"
  },
  "devDependencies": {
    "serverless-offline": "^5.8.0"
  },
  "scripts": {
    "start": "ghcid --command='stack ghci multi-targeter:test:multi-targeter-test' --test=main"
  }
}
